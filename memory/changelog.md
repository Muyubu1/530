# Changelog

- feat(landing): Sistem bölümü düzenlemesi + Davet'e el-çizimi yıldız. Pillar 03 "KARDEŞLİK"→"BİRLİK" (hero eyebrow + footer triad'ı hâlâ "DİSİPLİN · İNANÇ · KARDEŞLİK" — kullanıcı sadece pillar'ı istedi). "NE ALIYORSUN" bloğu (5.30 protokolü/Kardeşlik/Hesap verme/Eğitim&görev) tamamen silindi. Yeni `ui/design-system/backgrounds/hand-drawn-star.tsx` (`HandDrawnStar`): tek-stroke pentagram, scroll'a girince stroke-dashoffset ile çizilir — verilen framer-motion bileşeni yerine GSAP'la yapıldı (proje GSAP tabanlı + perf; yeni runtime eklenmedi, `.hairline` deseni). Davet bölümünde (10) kartın arkasında ortalı (`zIndex:0`, kart `zIndex:1`) → uçlar kartın etrafından radyal taşar (premium rozet hissi). reduced-motion'da statik çizili. typecheck/lint temiz; dev 5173 200. NOT: yıldız boyutu/konumu (`min(96vw,1080px)`) görsel ayar gerektirebilir.
- perf(landing): landing görselleri WebP'e indirildi + InkReveal mobilde kaldırıldı/canvas hafifletildi. Sebep: yavaşlık kaygısı (asıl yük 7MB landing görseli, 1.jpeg 2.5MB@2752px; animasyonlar zaten GPU-dostu transform/opacity) + mobilde hover olmadığı için gölge oyunu anlamsız. `public/landing/1–8.webp` (`cwebp -q 80 -resize 2048 0`): 7.0MB → 564KB (1.jpeg 2.5MB→51KB); orijinal .jpeg'ler yedek kaldı. img `src` `.jpeg`→`.webp`, ilkine `fetchPriority="high"`. InkReveal: `(hover:none)` → `return null` (mobilde hiç canvas yok, sıfır overhead), DPR cap 2→1.5 (blur(1px)+gradient ile ayırt edilemez, ~%44 az piksel), maxStamps 340→200. Tasarım değişmedi (görsel eşdeğer). typecheck/lint temiz; dev 5173 webp 200. content-visibility GSAP ölçüm riski nedeniyle uygulanmadı.
- feat(landing): hero "mürekkep keşfi" — `cine-stage`'in tüm tırmanışına interaktif karanlık keşif veil'i. Sebep: kullanıcı açılış sahnesini "etraf karanlık, 5.30 loş görünür, sayfayı görmek için mouse'la dolaşmak gerek" konseptine çevirmek istedi; kaydırınca gelen akışlar da mouse ile keşfedilsin. Yeni bileşen `ui/design-system/backgrounds/ink-reveal.tsx` (21st.dev ink-reveal'den uyarlandı): flat opak maske DEĞİL → koyu yarı-saydam veil (`rgba(7,10,14,0.78)`), destination-out ile büyük (brush 220) + kalıcıya yakın (lifetime 1100, maxStamps 340) wobble'lı mürekkep lekeleri; gradient merkez stop 0.85 → merkez bile %100 açılmaz (residual loşluk), canvas `blur(1px)` ile yumuşak/bulanık kenar. cursor normal. Touch (`onTouch*`, passive, scroll bozmaz) + `prefers-reduced-motion`/`(hover:none)` → veilAlpha 0.22 + reduced'da statik (loop yok) → mobilde sahne görünür kalır. `cine-stage` son child'ı (HUD'dan önce), `zIndex:30` tüm metin+8 görseli kaplar; fixed `/login` linki üstte tıklanabilir. index.ts Backgrounds export. typecheck temiz; dev 5173 SSR 200. branch: feat/hero-ink-reveal. NOT: tarayıcı görsel kalibrasyonu (veilAlpha/brush dengesi) bekliyor.
- fix(landing): akış sıralaması (pin) düzeltildi — KRİTİK. Sebep: pin yanlış elemana uygulanmıştı (`pin: ".cine-stage"` = child), trigger `.cine-section` sabit `height:100vh` olduğu için ScrollTrigger pin-spacer'ı section içine koyup kırpılıyordu → içerik bölümleri (08-11) aşağı itilemiyor, sabitlenmiş stage'in üstüne boyanıyor; sıra bozuk (Hero→içerik→sonra tırmanış, tırmanış "hızlı") oluyordu. Çözüm: `pin: true` (section'ın kendisi pinlenir, kök seviyede doğru spacer) + `anticipatePin: 1` (Lenis ile pin zıplaması yok) + setup sonunda `ScrollTrigger.refresh()`. Sıra düzeldi: Hero → tüm tırmanış (DİPTE→SIRA SENDE) → Sistem/Eleme/Davet/Başvur → footer; tırmanış tam scroll uzunluğunu geri kazandı. typecheck/lint/build temiz.
- fix(landing): metin çakışması + hayalet numara + tempo. Sebep: geçişte iki altyazı üst üste biniyordu (seg i çıkışı t+0.4..t+0.95, seg i+1 girişi t+0.5 → örtüşme) ve dev "08/09" watermark orta-ekrana sürüklenip akışı bozuyordu. Çözüm: metin zamanlaması görsel crossfade'den AYRIŞTIRILDI — seg container opacity:1 sabit, görünürlüğü `.ln` satırları taşır; her altyazı bir sonraki girmeden TAM temizlenir (giriş t-0.42 stagger 0.06 dur0.5 expo.out; çıkış t+0.42 dur0.22 opacity-only, komşuya iten yPercent çıkış drifti kaldırıldı). stair-word kendi opacity in/out'u (seg değil), drift 1.05 beat. Tempo: `BEAT=1.25` (beat aralığı açıldı, çakışmasız boşluk) + beat-normalize scroll `end=TOTAL*VH_PER_BEAT*innerHeight` (full 1.7 / lite 1.2 / reduced 1.4 ekran/beat) → tutarlı rahat tempo; scrub 1.2→1.1, Lenis lerp 0.075→0.08. Hayalet no: sectionGhost `bottom:-8% right:1%`, fontSize 300px, opacity 0.016, `zIndex:0`; içerik sarmalı `zIndex:1`; parallax ±8→±4. typecheck/lint/build temiz. NOT: tarayıcı görsel doğrulaması bekliyor (Playwright kullanıcı onayı).
- feat(landing): sinematik tırmanış GSAP ScrollTrigger'a taşındı + tüm sayfa profesyonelleştirildi. Sebep: el-yapımı rAF motoru "slayt" hissi veriyordu (eşit aralık, keskin durup crossfade, kamera her görselde sıfırlanıyor). Yeni: tek pinned timeline (scrub), sürekli kamera dolly (scale 1.06→1.34, hiç durmaz), uzun örtüşen cross-dissolve + giden görselde blur/scale push-through, per-frame Ken Burns (sonsuz), birleşik film grade (dipte soğuk/karanlık → zirvede açık/sıcak: bright-overlay düşer + cine-grade-warm yükselir), sahne-senkron light-leak, eşit-olmayan ritim (HOLD_LEAD/HOLD_TAIL). Pointer parallax (gsap.quickTo: world rotX/Y + fx + text). Lenis artık gsap.ticker'a bağlı (lagSmoothing 0). `gsap.matchMedia` ile 3 katman: full (desktop) / lite (touch, blursuz) / reduced (sadece opacity, Lenis yok). Alt 4 bölüm: ad-hoc `.reveal` → design-system `Reveal` varyantları (fade-down/blur-in/letter-rise/slide/scale-in/fade-up), `.parallax-slow` dev bölüm-no hayaleti + `.hairline` scaleX çizimi (scrubbed ScrollTrigger), Davet kartı pointer 3D tilt. Reveal primitive'e opsiyonel `style` prop eklendi (anim altına merge). ScrollGlow eklenmedi (z-0 screen-blend climb üstüne mavi sızıntı riski). Dep: gsap 3.15 + @gsap/react 2.1. typecheck/lint/build(SSR) temiz; pin manuel yükseklik yerine ScrollTrigger ile, cine-section 100vh. Detay: decisions D11 güncellendi.
- feat(landing): ana sayfa (`/`) tamamen yeni sinematik landing ile değiştirildi (530V2 portu). Sebep: yeni immersive landing akışı. Pinned 3D tırmanış (8 görsel `public/landing/1–8.jpeg`, Lenis smooth scroll, rAF tek-döngü: parallax/motes/grain/light-leak/HUD + per-line reveal) + 4 bölüm (Nedir/Eleme/Davet/Başvur). Eski sections/components (hero/interlude/vsl/barcode-cta) silindi. Header'sız immersive, sağ üstte diskret `/login` linki. Fontlar (Anton/Archivo/Space Mono/Playfair) `__root.tsx`'e, keyframe+scope `styles.css`'e (`.cine-landing`). Başvuru formu mevcut waitlist backend'e bağlandı: NewWaitlistEntry'ye `contact`+`why` eklendi, email opsiyonel; submitWaitlist contact email-benzeriyse email türetir. waitlist tablosuna contact/why kolonları + email nullable + RLS gevşetildi (setup.sql + migration 0007). Admin bekleme-listesi tablo/CSV iletişim+neden gösterir. typecheck/lint/build temiz. `lenis` dep eklendi. DİKKAT: 0007 SQL Supabase'e uygulanmalı (IG-only contact için).
- chore: refactor530 iskelesi kuruldu — TanStack Start + React 19 + Tailwind v4, Lovable bağımlılıkları çıkarıldı. Sebep: temiz/SOLID temel. package.json, vite/ts/eslint config
- feat: tasarım tokenları tek kaynağa taşındı. Sebep: DRY, modül-modül tasarım dili. src/ui/design-system/tokens.css + src/styles.css
- feat: app shell (router, \_\_root, index, playground). Sebep: çalışan SSR temeli. src/router.tsx, src/routes/\*
- chore: kalite kapıları — eslint-plugin-boundaries (katman zorlaması, warn), prettier, vitest. Sebep: mimari kuralla korunmalı. eslint.config.js
- feat: tasarım sistemi primitive katmanı — Button(cva), Eyebrow, Heading/GradientText, Section/Container, Card, FeatureList, GlowHalo, Divider, Wordmark, Reveal + Backgrounds taşındı. Sebep: inline/tekrarlı stilleri ortak modüle indir. src/ui/design-system/\*
- feat: /playground bileşen kataloğu — tüm primitive'ler görsel doğrulama. SSR 200, typecheck+lint temiz.
- feat: tasarım sistemi pattern katmanı — PageHero, PricingCard, MediaCard (dashboard 2x kopyanın yerine), EventCard, CinematicVideoFrame. Sebep: feature-seviyesi ortak modüller; ekranlar diziye dizecek. src/ui/design-system/patterns/\*
- feat(faz2): landing (ana) yeni modüllerle kuruldu — SiteHeader/SiteFooter (ui/shared), ProgramScene, HeroParticles/ScrollGlow (hidrasyon-güvenli), Dialog primitive, BarcodeCTA, WaitlistForm (onSubmit enjekte=DIP). pricing domain'e taşındı. Hero/Interlude/Vsl section'ları features/marketing. Stripe/Supabase Faz 3 stub. SSR 200.
- feat(faz2): program sayfaları — kisisel (PricingCard + upsell accordion), ozel (radio plan seçici), mentorluk (VSL + Neden Mentör). features/programs; ince route'lar checkout nav'ı enjekte eder; ProgramScene 3x tekrar tek modüle indi. /odeme + satin-al stub'ları (Faz 3). 8 route SSR 200.
- feat(faz13): admin/üye AYRIMI + admin dashboard + içerik yönetimi (CMS). ValidatedField şifre-göster (göz). server requireAdmin(token) (user_roles admin kontrolü) — tüm write fn'leri gate'ler. login/signup rol-bazlı yönlendirme (admin→/admin, üye→/uye). `/admin` gated AdminLayout (sidebar dashboard, mevcut mono-sinema dili) + genel bakış (adminStatsFn). bekleme-listesi+fiyat-kontrol /admin'e taşındı. TAM CRUD: course/lesson/event/update/material (domain port + postgres adapter write metotları + admin-content.ts fn'leri + CMS ekranları: kurslar/$courseId ders+materyal, etkinlikler, güncellemeler). URL-tabanlı medya. Üye tarafı değişmedi; admin yayınlayınca anında görünür. Demo üye: uye.530.demo@gmail.com/Uye.530.2026 (member); admin.530.demo@gmail.com/Admin.530.2026 (admin). Üye GoTrue email limiti aşıldığında direkt SQL ile oluşturuldu (G10).
- feat(routing+seed): Landing köke alındı (`/` = landing; geçici hub → `/gecicihub`; `/ana`→`/` redirect). Zengin Türkçe tohum (0006_seed_rich): +2 kurs (Beden & Antrenman, Zihin & Odak), 29 ders (Picsum görsel + örnek video), 7 etkinlik, 6 güncelleme, 6 materyal. supabase/seed-community.sql: 7 demo mesaj + 6 reaksiyon. Hepsi yeni Supabase'e.
- chore(db): TEK BACKEND = yeni Supabase. Lokal Postgres bırakıldı; DATABASE_URL → Supabase pooler (aws-1-ap-south-1). setup.sql (Supabase-native şema) + migrations 0002–0005 yeni projeye uygulandı; demo admin oluşturuldu (admin.530.demo@gmail.com). prepare:false (pooler). Sıradaki: zengin tohum + Vercel deploy. Detay: gotchas G10.
- feat(patika-demo): izole, interaktif Patika demo ekranı (`/patika-demo`, public, ana akışa bağlı değil). Mock + localStorage; ui/features/patika-demo (mock-journey + use-demo-journey hook + bileşenler: stat-bar, journey-map serpentine SVG yol, day-node, today-panel, task-row işaret+not, badge-shelf, reward-modal Dialog). 28 gün, görev işaretle→gün tamamla→seri/puan→milestone rozet (7/14/21/28). Mono-sinema diline uyumlu (docs/tasarim-sistemi.md). dev hub'a link. Backend/domain YOK — proje sahibi görüşmesi öncesi his/test amaçlı.
- docs: docs/patika-sorular.md (proje sahibi görüşme soruları) + docs/tasarim-sistemi.md (tasarım sistemi tam referansı).
- feat(faz9): fiyat-kontrol (admin Stripe fiyat denetimi). domain/pricing-audit (PriceCatalog portu + PriceAuditRow) + application/audit-pricing + infra stripe-price-catalog (prices.list lookup_key) + ortak stripe.server.ts (DRY: lazy getStripeClient, payment-gateway de kullanır) + auditPricingFn. UI pricing-audit-page (admin-gate; match/mismatch/missing rozetleri; anahtar yoksa "yapılandırma eksik") + route + profil kartı. Saf birim test (3 durum). Eski sistemin son görünür ekranı — existing TAMAM. NOT: client'taki "STRIPE_SECRET_KEY" string'i sadece UI etiketi, sızıntı değil (server adapter strip edildi).
- feat(faz8): üye ertelenenler. Materyaller (0005 migration lesson_materials + domain/app/infra/server + course-detail MaterialsPanel, public URL indirme, format-bytes). Avatar yükleme (AuthGateway.uploadAvatar → avatars bucket + auth metadata; AuthUser.avatarUrl; ayarlar avatar bölümü; member-layout/profil/chat yansıma). Admin bekleme-listesi (AuthGateway.isAdmin via user_roles + listWaitlist; waitlist-admin-page tablo+CSV; profil kartı koşullu). Abonelik sade ekran. fiyat-kontrol ertelendi (Stripe). typecheck/lint/build/SSR temiz, 12 test.
- feat(faz7): Stripe ödeme akışı (sayfa içi Payment Element). domain payment/purchase portları; infra Stripe gateway (tutar domain/pricing'den, Stripe'ta ürün gerekmez) + Supabase purchase repo (anon insert, RLS); application + server fns (createPaymentIntent/recordPurchase); ui/shared/stripe (lazy client + Payment Element formu + test banner); features/payment (PaymentPage + ThankYouPage). Route'lar: kisisel/ozel odeme→payment-success→signup (gate açılır), mentorluk→tesekkurler. CheckoutStub silindi. Webhook/taksit ertelendi. Anahtarlar (VITE_STRIPE_PUBLISHABLE_KEY+STRIPE_SECRET_KEY) sonra; pk/sk yokken "yapılandırma eksik" gösterir. Birim test + build temiz (secret sızmıyor).
- feat(faz6 B/C/D): topluluk chat TAM bitti. B: realtime reaksiyonlar + yanıtla + context menu + pin. C: görsel yükleme + photo editor (crop/yazı) + sesli mesaj + audio waveform player + image viewer. D: emoji-only büyük baloncuk, mesaj slide-in animasyonu, swipe-to-reply, swipe-to-close viewer, visualViewport klavye + body scroll-lock, hoş geldin notu. Hepsi mono-sinema dili; ~20 modüler bileşen. typecheck/lint/build/SSR temiz.
- feat(faz6a): topluluk chat çekirdeği — realtime mesajlaşma. domain/chat (ChatGateway portu), ui/shared/chat Supabase adapter (browser, RLS, realtime postgres_changes + storage), use-chat hook, modüler bileşenler (message-list/row/bubble/avatar/date-separator/composer/scroll-FAB) + chat-page. mono-sinema dili (mavi Telegram değil). MemberLayout isChat (tam ekran) + topluluk nav aktif. community_messages/message_reactions tabloları mevcut Supabase'den. /uye/topluluk SSR 200. Reaksiyon/medya/cila (B/C/D) sırada.
- feat(faz5): üye alanı tamamlandı — kullanıcı-özel veri (completed_lessons/saved_lessons/lesson_notes) lokal Postgres + token-doğrulamalı server fn'ler (`server/auth.ts` verifyUser, `AuthGateway.getAccessToken`). Ders detayı: tamamla/kaydet/notlar + watch-progress + tamamlandı rozetleri (CinematicVideoFrame onTimeUpdate/startAt/topRightSlot ile genişletildi). Profil hub + notlar/videolar/ayarlar + güncellemeler(updates tablosu)+ kütüphane. "benim odam" nav aktif. Input/Label/Checkbox zaten vardı. 0004 migration. 3 yeni entegrasyon testi (9 toplam). Materyaller/avatar/abonelik/admin ertelendi.
- feat(faz4): Supabase auth + waitlist adapter swap. domain/auth (AuthGateway portu) + ui-shared client adapter (lazy browser client, SSR-safe) + AuthProvider/useAuth. 4 ekran (login/signup[satın-alma ön-kontrol]/forgot/reset) + Input/Label/Checkbox primitives. /uye satın-alma şartlı gate (email_has_purchase); mock session silindi (AuthUser'a geçildi). Waitlist Postgres→Supabase tek-satır composition swap (Postgres adapter+test referans kaldı). Google OAuth (native). typecheck/lint temiz; tüm route SSR 200; build'de secret/driver client'a sızmıyor.
- feat(faz3b): üye alanı — content veri katmanı (courses/lessons/events) aynı ports&adapters deseniyle. domain (course/event/session), application (list-courses/get-course/list-events), Postgres adapter'ları, server fns (createServerFn GET + null-on-notfound), mock dev-session (server/session). UI: MemberLayout (ui/shared) + DropdownMenu/LessonThumb primitives + 4 ekran (dashboard/courses/course-detail/events) hazır modüllerle. Route'lar loader→server fn (projede ilk loader kullanımı). 0002_content+0003_seed migration. 6 entegrasyon testi geçti; /uye SSR 200, 404 doğru; build client temiz; date-fns Date serialization sorunsuz.
- feat(faz3a): lokal Postgres veri katmanı — ports&adapters. domain/waitlist (port+entity+DuplicateEmailError), application/submit-waitlist (use-case), infrastructure (PostgresWaitlistRepository + db client, .server.ts), server/waitlist (createServerFn composition root). WaitlistForm gerçek insert'e bağlandı (stub kalktı). migration runner (scripts/migrate.mjs) + 0001_waitlist. boundaries warn→ERROR + server element. Entegrasyon testi (gerçek DB) geçti; build'de client'a sızıntı yok.
